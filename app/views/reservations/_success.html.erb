<div class="p-6 text-center">
  <div class="mb-6">
    <div class="mx-auto flex items-center justify-center h-16 w-16 rounded-full bg-green-100 mb-4">
      <svg class="h-8 w-8 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
      </svg>
    </div>
    <h2 class="text-2xl font-bold text-green-700 mb-2">예약이 완료되었습니다! 🎉</h2>
    <p class="text-gray-600">입력하신 정보로 코치가 곧 연락드립니다.</p>
  </div>
  
  <!-- 예약 정보 요약 -->
  <div class="bg-gradient-to-br from-gray-50 to-indigo-50 rounded-2xl p-6 mb-6 text-left shadow-inner">
    <h3 class="text-lg font-semibold text-gray-900 mb-4">예약 정보</h3>
    <dl class="space-y-3 text-base">
      <div class="flex justify-between">
        <dt class="text-gray-700">이름</dt>
        <dd class="font-semibold text-gray-900"><%= reservation.name %></dd>
      </div>
      <div class="flex justify-between">
        <dt class="text-gray-700">연락처</dt>
        <dd class="font-semibold text-gray-900"><%= reservation.phone %></dd>
      </div>
      <div class="flex justify-between">
        <dt class="text-gray-700">이메일</dt>
        <dd class="font-semibold text-gray-900"><%= reservation.email %></dd>
      </div>
      <div class="flex justify-between">
        <dt class="text-gray-700">예약 일시</dt>
        <dd class="font-semibold text-gray-900">
          <%= reservation.reservation_datetime.strftime("%Y년 %m월 %d일 %H시 %M분") %>
        </dd>
      </div>
      <div class="flex justify-between">
        <dt class="text-gray-700">코칭 형태</dt>
        <dd class="font-semibold text-gray-900"><%= reservation.coaching_type %></dd>
      </div>
      <% if reservation.selected_subjects.any? %>
        <div class="flex justify-between">
          <dt class="text-gray-700">선택 과목</dt>
          <dd class="font-semibold text-gray-900"><%= reservation.selected_subjects.join(", ") %></dd>
        </div>
      <% end %>
      <% if reservation.requests.present? %>
        <div class="flex justify-between">
          <dt class="text-gray-700">요청사항</dt>
          <dd class="font-semibold text-gray-900"><%= reservation.requests %></dd>
        </div>
      <% end %>
    </dl>
  </div>
  
  <!-- 안내 메시지 -->
  <div class="bg-blue-50 border-2 border-blue-200 rounded-2xl p-4 mb-6">
    <p class="text-sm text-blue-800">
      예약 일시 24시간 전에 리마인더 메시지를 발송해드립니다.
    </p>
  </div>
  
  <!-- 홈으로 돌아가기 -->
  <%= link_to "홈으로 돌아가기", root_path, 
              data: { turbo_frame: "_top" },
              class: "inline-block bg-indigo-600 text-white py-4 px-6 rounded-2xl font-semibold shadow-lg shadow-indigo-600/20 hover:bg-indigo-700 transition-all transform active:scale-[0.98]" %>
</div>
